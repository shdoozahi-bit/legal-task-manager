rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes to the app collection
    // Note: This is open access — suitable for single-user personal app
    // To restrict later: add Firebase Authentication
    match /app/{document} {
      allow read, write: if true;
    }
  }
}
